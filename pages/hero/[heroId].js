import HeroPage from "../../components/HeroPage";

const Hero = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        minHeight: "100vh",
        justifyContent: "center",
      }}
    >
      <HeroPage/>
    </div>
  );
};

export default Hero;
